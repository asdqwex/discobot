machine:
  services:
    - docker

test:
  override:
    - docker login -e="." -u="${DOCKER_USERNAME}" -p="${DOCKER_PASSWORD}" quay.io
    - make pull || true
    - make

deployment:
  master:
    branch: master
    commands:
      - make push
