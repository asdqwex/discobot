machine:
  services:
    - docker

dependencies:
  override:
    - echo "skip deps"

test:
  override:
    - docker login -e="." -u="${DOCKER_USERNAME}" -p="${DOCKER_PASSWORD}" quay.io
    - make pull || true
    - make

deployment:
  docker:
    branch: /.*/
    commands:
      - make push
